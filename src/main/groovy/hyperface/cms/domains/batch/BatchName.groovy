package hyperface.cms.domains.batch

/**
 * Enum to hold names of different batches that can serve various purpose like:
 * 1. Updating currency_conversion_table
 * 2. Updating Blacklisted Bin table
 */
enum BatchName {
    CURRENCY_CONVERSION;
}